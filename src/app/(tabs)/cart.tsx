import { PlaceholderScreen } from '@/components/placeholder-screen';

/** Cart tab — the basket before checkout. Not built yet. */
export default function CartScreen() {
  return (
    <PlaceholderScreen
      icon="cart-outline"
      title="Your cart is empty"
      message="Prints you add will collect here, ready for checkout."
    />
  );
}
